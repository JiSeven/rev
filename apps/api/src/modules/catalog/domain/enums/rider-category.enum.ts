/**
 * EU motorcycle licence categories.
 * A1 — up to 125cc, max 11kW
 * A2 — up to 35kW, power/weight ≤ 0.2 kW/kg
 * A  — unrestricted
 */
export enum RiderCategory {
  A1 = 'A1',
  A2 = 'A2',
  A = 'A',
}
